---
const {
  title = "Morse Code Generator",
  description = "Translate text to Morse code instantly, learn the alphabet, and practice common phrases.",
} = Astro.props;

const site =
  Astro.site?.toString().replace(/\/$/, "") ?? "https://morsecodegenerator.com";
const path = Astro.url.pathname || "/";
const canonical = `${site}${path === "/" ? "/" : path}`;

// Minimal JSON-LD to help SEO + AI systems understand the site
const jsonLd = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Organization",
      "@id": `${site}#org`,
      name: "MorseCodeGenerator.com",
      url: site,
      email: "morsecodegenerator@gmail.com",
    },
    {
      "@type": "WebSite",
      "@id": `${site}#website`,
      url: site,
      name: "Morse Code Generator",
      publisher: { "@id": `${site}#org` },
      potentialAction: {
        "@type": "SearchAction",
        target: `${site}/translate/?q={search_term_string}`,
        "query-input": "required name=search_term_string",
      },
    },
  ],
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="referrer" content="strict-origin-when-cross-origin" />

    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <meta name="robots" content="index,follow" />

    <!-- Favicon -->
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="alternate icon" href="/favicon.ico" />

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:type" content="website" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />

    <meta name="application-name" content="Morse Code Generator" />
    <script type="application/ld+json">{JSON.stringify(jsonLd)}</script>

    <!-- Google AdSense (loader only) -->
    <script
      async
      src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-7616383336440831"
      crossorigin="anonymous"
    ></script>

    <!-- GA4 -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-1K9JZ9ZQ2L"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-1K9JZ9ZQ2L', { anonymize_ip: true });
    </script>
  </head>

  <body>
    <a class="skip" href="#content">Skip to content</a>

    <header class="site-header">
      <div class="wrap header-inner">
        <a class="brand" href="/">MorseCodeGenerator.com</a>

        <nav class="nav" aria-label="Primary">
          <a class="nav-link" href="/translate/">Translate</a>
          <a class="nav-link" href="/morse-code/a/">Alphabet</a>
          <a class="nav-link" href="/phrases/sos/">Phrases</a>
          <a class="nav-link contact" href="mailto:morsecodegenerator@gmail.com" aria-label="Email MorseCodeGenerator.com">
            Contact
          </a>
        </nav>
      </div>
    </header>

    <main id="content" class="wrap">
      <div class="panel">
        <slot />
      </div>
    </main>

    <footer class="site-footer">
      <div class="wrap foot-inner">
        <small class="foot-text">
          © {new Date().getFullYear()} MorseCodeGenerator.com • Free Morse translator, references & learning tools
        </small>
        <a class="foot-contact" href="mailto:morsecodegenerator@gmail.com">morsecodegenerator@gmail.com</a>
      </div>
    </footer>

    <style>
      :root {
        color-scheme: light dark;
        --bg: #0b0b0b;
        --panel: rgba(255,255,255,0.040);
        --panel2: rgba(255,255,255,0.020);
        --border: rgba(255,255,255,0.10);
        --border2: rgba(255,255,255,0.16);
        --text: rgba(255,255,255,0.94);
        --muted: rgba(255,255,255,0.80);
        --subtle: rgba(255,255,255,0.65);
        --accent: #8ab4ff;
        --accentBg: rgba(138,180,255,0.16);
        --shadow: 0 10px 30px rgba(0,0,0,0.35);
      }

      * { box-sizing: border-box; }
      html, body { height: 100%; }

      body {
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
        margin: 0;
        line-height: 1.6;
        background:
          radial-gradient(1200px 600px at 50% -10%, rgba(138,180,255,0.10), transparent 60%),
          var(--bg);
        color: var(--text);
      }

      /* Skip link (accessibility) */
      .skip {
        position: absolute;
        left: -999px;
        top: 10px;
        padding: 10px 12px;
        background: rgba(0,0,0,0.85);
        border: 1px solid var(--border);
        border-radius: 10px;
        color: var(--text);
        text-decoration: none;
        z-index: 999;
      }
      .skip:focus { left: 12px; }

      a { color: var(--accent); text-underline-offset: 3px; }
      a:hover { opacity: 0.92; }

      .wrap {
        max-width: 1040px;
        margin-left: auto;
        margin-right: auto;
        padding: 20px;
      }

      /* Header */
      .site-header {
        position: sticky;
        top: 0;
        z-index: 20;
        backdrop-filter: blur(10px);
        background: rgba(11,11,11,0.78);
        border-bottom: 1px solid var(--border);
      }

      .header-inner {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 14px;
      }

      .brand {
        font-weight: 760;
        letter-spacing: 0.2px;
        text-decoration: none;
        color: var(--text);
        white-space: nowrap;
      }

      .nav {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        justify-content: flex-end;
        align-items: center;
      }

      .nav-link {
        text-decoration: none;
        color: var(--text);
        padding: 8px 12px;
        border-radius: 999px;
        border: 1px solid rgba(255,255,255,0.10);
        background: rgba(255,255,255,0.03);
      }

      .nav-link:hover {
        border-color: rgba(255,255,255,0.16);
        background: rgba(255,255,255,0.05);
      }

      .nav-link.contact {
        border-color: rgba(138,180,255,0.40);
        background: var(--accentBg);
      }

      /* Main panel */
      .panel {
        background: linear-gradient(to bottom, var(--panel), var(--panel2));
        border: 1px solid var(--border);
        border-radius: 18px;
        box-shadow: var(--shadow);
        padding: 24px;
      }

      /* Typography defaults inside pages */
      h1 { line-height: 1.15; margin-top: 6px; }
      h2 { line-height: 1.2; margin-top: 26px; }
      p { color: var(--text); }
      .muted { color: var(--muted); }
      .k { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; }

      /* Forms */
      textarea, input {
        width: 100%;
        padding: 12px 12px;
        border-radius: 12px;
        border: 1px solid var(--border2);
        background: rgba(0,0,0,0.18);
        color: var(--text);
        outline: none;
      }
      textarea::placeholder, input::placeholder { color: rgba(255,255,255,0.55); }
      textarea:focus, input:focus {
        border-color: rgba(138,180,255,0.55);
        box-shadow: 0 0 0 4px rgba(138,180,255,0.10);
      }

      button {
        padding: 10px 12px;
        border-radius: 12px;
        border: 1px solid rgba(138,180,255,0.35);
        background: var(--accentBg);
        color: var(--text);
        cursor: pointer;
        font-weight: 650;
      }
      button:hover { opacity: 0.96; }
      button:active { transform: translateY(1px); }

      /* Cards + grids */
      .card {
        border: 1px solid var(--border);
        border-radius: 16px;
        padding: 16px;
        background: rgba(255,255,255,0.02);
      }

      .grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: 14px;
      }
      @media (min-width: 860px) {
        .grid { grid-template-columns: 1.2fr .8fr; }
      }

      /* Nice tables for numbers/punctuation pages */
      table { width: 100%; border-collapse: collapse; }
      th, td {
        padding: 10px;
        border-bottom: 1px solid rgba(255,255,255,0.10);
        text-align: left;
      }
      th { color: rgba(255,255,255,0.85); font-weight: 700; }

      /* Utility: pill grid (use for phrases list) */
      .pill-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 10px;
        list-style: none;
        padding-left: 0;
        margin: 0;
      }
      @media (min-width: 720px) {
        .pill-grid { grid-template-columns: repeat(4, minmax(0, 1fr)); }
      }
      .pill-grid a {
        display: block;
        text-decoration: none;
        color: var(--text);
        padding: 10px 12px;
        border-radius: 12px;
        border: 1px solid rgba(255,255,255,0.10);
        background: rgba(255,255,255,0.03);
      }
      .pill-grid a:hover {
        border-color: rgba(138,180,255,0.35);
        background: rgba(138,180,255,0.10);
      }

      /* Footer */
      .site-footer {
        border-top: 1px solid var(--border);
        margin-top: 26px;
        padding-bottom: 30px;
      }

      .foot-inner {
        display: flex;
        flex-wrap: wrap;
        gap: 10px 16px;
        align-items: center;
        justify-content: space-between;
      }

      .foot-text { color: var(--subtle); }
      .foot-contact {
        color: var(--accent);
        text-decoration: none;
        padding: 8px 12px;
        border-radius: 999px;
        border: 1px solid rgba(255,255,255,0.10);
        background: rgba(255,255,255,0.03);
      }

      /* Mobile */
      @media (max-width: 640px) {
        .wrap { padding: 14px; }
        .panel { padding: 16px; }
        .header-inner { align-items: flex-start; flex-direction: column; }
        .nav { justify-content: flex-start; width: 100%; }
      }
    </style>
  </body>
</html>
